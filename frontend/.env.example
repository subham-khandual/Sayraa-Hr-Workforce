# Frontend environment variables (copy to .env and fill with real values)

# Local dev — Vite proxy handles /api in development, but these are used as fallbacks
VITE_API_URL=http://localhost:5000/api/v1
VITE_WS_URL=ws://localhost:5000/ws/interview
